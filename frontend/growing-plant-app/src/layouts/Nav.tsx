import React from 'react';
import MainDrawer from '../components/drawers/MainDrawer';

const Nav = (): JSX.Element => {

    return (
        <nav>
            <MainDrawer/>
        </nav>
    );
};

export default Nav;